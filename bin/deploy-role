#!/bin/bash

self_path="$(readlink -f ${0})"
bin_dir="${self_path%/*}"
env_dir="${bin_dir%/*}"

cd "${env_dir}"

libs=(
  'output'
  'paths'
  'inventory'
  'deploy_role'
)
for lib in "${libs[@]}"; do
  . lib/${lib}.sh
done


deploy_role "${@}"
