#!/bin/bash

self_path="$(readlink -f ${0})"
bin_dir="${self_path%/*}"
env_dir="${bin_dir%/*}"
role_dir="${env_dir%/*}"

cd "${env_dir}"

libs=(
  'paths'
)
for lib in "${libs[@]}"; do
  . lib/${lib}.sh
done

ansible-inventory -i "${inventory_path}/inventory.d/" --list
