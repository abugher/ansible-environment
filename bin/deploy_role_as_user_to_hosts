#!/bin/bash
role="${1}"
shift
user="${1}"
shift
target_hosts="${1}"
shift
time ansible-playbook ./playbooks/deploy_role_as_user_to_hosts.yml -e role="${role}" -e user="${user}" -e target_hosts="${target_hosts}" "${@}"
